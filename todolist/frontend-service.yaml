apiVersion: v1
kind: Service
metadata:
  name: todolist-frontend
  namespace: todolist
spec:
  selector:
    app: todolist-frontend
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: ClusterIP

